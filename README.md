# interaction-clustering
Building a publication-ready pilot that tests whether short, single-turn user messages can be grouped into meaningful interaction-level patterns using one embedding model + unsupervised clustering (KMeans, HDBSCAN).
